user_xikis = {}

getuser = (req) ->
  # get user either from request or
  # get user from environment (running this process)


class BaseXiki

class Xiki
  constructor: (opts) ->
    {@credentials} = opts

  open: (path, body="", params={}) ->
    




module.exports =
  __express: (req, res) ->
    path = req.params[0]
    # req.query has parameters
    if typeof req.body == "string"
